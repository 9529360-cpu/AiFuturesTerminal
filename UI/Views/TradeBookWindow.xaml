<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="AiFuturesTerminal.UI.Views.TradeBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trade Book" Height="450" Width="800">
    <Grid>
        <DockPanel LastChildFill="True" Margin="0">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="8">
                <Button Content="Refresh" Margin="4,0,4,0" Command="{Binding RefreshCommand}" />
                <TextBlock Text="{Binding SourceText}" Margin="4,0,4,0" VerticalAlignment="Center" />
                <TextBlock Text="{Binding LastEventText}" Margin="8,0,4,0" VerticalAlignment="Center" TextWrapping="Wrap" />
            </StackPanel>

            <DataGrid ItemsSource="{Binding Trades}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time"
                                        Binding="{Binding Time}"
                                        Width="*" />
                    <DataGridTextColumn Header="Symbol"
                                        Binding="{Binding Symbol}"
                                        Width="*" />
                    <DataGridTextColumn Header="Side"
                                        Binding="{Binding Side}"
                                        Width="*" />
                    <DataGridTextColumn Header="Mode"
                                        Binding="{Binding ModeText}"
                                        Width="80" />
                    <DataGridTextColumn Header="Quantity"
                                        Binding="{Binding Quantity}"
                                        Width="*" />
                    <DataGridTextColumn Header="Entry Price"
                                        Binding="{Binding EntryPrice}"
                                        Width="*" />
                    <DataGridTextColumn Header="Exit Price"
                                        Binding="{Binding ExitPrice}"
                                        Width="*" />
                    <DataGridTextColumn Header="PnL (USDT)"
                                        Binding="{Binding Pnl}"
                                        Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Window>